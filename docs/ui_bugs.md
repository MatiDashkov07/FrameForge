- when no reference images are present in the references tab. the placeholder text is not centered in the middle but ib the lower middle of the canvas area.

- redesign the referene image canvas area, that when images are present, instead of having rows of a small image plus the name of the image, make it similar to how pintrest looks, with for example 2 big pictures in each row, and on hover, it shows the name of the picture plus the delete button.

- when an image is first rendered when it arrives back, it doesnt fill the whole canvas area, when switching after that to the sketch and back to the rendered photo, the rendered photo suddenly becomes the size of the sketch photo, make it consistent.

- make the application modular in size, so you'll be able to shrink and full screen it, currently the application cant scale up and down rendered photos, so if a picture takes the whole canvas you cant make the application's size smaller, make each picture modular in size, depending on the application's size.

- a general UI and UX redesign. finding an identity, colors, style, and making a new and improved layout, based on animation tools in the market.

